import { createEndpoint } from "@app/endpoint";

export default createEndpoint({
  POST: async (req, res) => {
    // Start game (once we receieve two requests to this route that are unique users, we will start the game)
    
  },
});
